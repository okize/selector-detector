Usage:

  selector-detector [URL] [OPTIONS]

Description:

  Count the CSS rules, selectors and declarations on a web page.

Options:

  -h, --help           Output usage information
  -V, --version        Output version number
  -p, --pretty         Output a 'pretty' version of results

Examples:

  $ selector-detector http://www.google.com
  $ selector-detector http://www.github.com -p